{
  "name": "mcp-ssh-proxy",
  "version": "1.0.0",
  "description": "MCP SSH Proxy - Electron App for manual SSH command approval",
  "main": "dist/mcp/server.js",
  "scripts": {
    "build": "tsc && shx mkdir -p dist/renderer && shx cp src/renderer/index.html dist/renderer/index.html && shx cp src/renderer/icon.svg dist/renderer/icon.svg",
    "start": "npm run build && electron dist/electron/main.js",
    "mcp": "node dist/mcp/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "electron-store": "^8.2.0",
    "ssh2": "^1.16.0",
    "sshpk": "^1.18.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/ssh2": "^1.15.1",
    "@types/ws": "^8.18.1",
    "electron": "^35.1.2",
    "shx": "^0.4.0",
    "typescript": "^5.8.3"
  }
}
